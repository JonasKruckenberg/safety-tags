[package]
name = "safety"
version = "0.1.0"
edition = "2021"
rust-version = "1.82" # for `unsafe extern` in generated code

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full", "visit-mut"] }
